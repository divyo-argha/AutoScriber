// autoScriber — real web app URL
//
// The full transcription studio (Next.js app) is deployed on the same
// server, served behind Caddy. Point the website's "Start in Browser"
// CTAs at the public URL where that app is reachable.
window.AUTOSCRIBER_APP_URL = '/app';